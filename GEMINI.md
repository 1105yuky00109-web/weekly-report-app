# 週次日報＆予定管理システム (weekly-report-app)

## 1. プロジェクト概要
本プロジェクトは、図面作図業務における各担当者の週次作業日報（実績）および月間作業予定（スケジュール）を効率的に登録・管理・集計するためのWebアプリケーションです。

### 主な機能
1. **認証機能 (Firebase Auth)**: 新規登録時の「フルネーム（氏名）」登録とプロフィール（DisplayName）紐付けに対応。メンバー自身によるアカウントの新規登録（方法B）およびログイン・ログアウト機能。
2. **予定登録＆ガントチャート**: 担当者ごとの工事予定を入力し、月間の作業予定をガントチャート（A3横印刷対応）で俯瞰する機能。日付入力用の便利なショートカットボタン（今日、明日、来週月曜）付き。
3. **実績入力（週次日報）**: 曜日ごとの稼働工事名・作業内容・稼働時間に加え、各曜日のカード内にその日の作業報告・備考を直接入力し、提出する機能（従来の画面最下部の週全体のまとめ欄は廃止）。
   - **工事名サジェスト**: 過去のデータから工事名を自動補完。
   - **日報コピー機能**: 過去に提出した日報から、作業内容と日次レポート内容をワンクリックで複製して入力時間を短縮。
   - **リアルタイム時間計算**: 各日の合計稼働時間、およびヘッダー部分の「今週の合計稼働時間」を即座に自動計算。
4. **個人別一覧・レポート（月間集計）**: 月単位での日報の確認、A4縦での印刷用レイアウト、および担当者別の月間工事稼働時間（合計）の自動集計表示。
5. **工事別集計**: 工事ごとに、どの週に誰が何時間作業したかを一元的に確認できる集計テーブル。
6. **Excelエクスポート**: 各一覧・集計データをボタン一つでExcelファイル（複数シート構成含む）に出力（SheetJSを使用）。
7. **マルチデバイス対応（iPad最適化）**: iPadやスマートフォンなどタッチ操作の多い端末でも崩れず使いやすいボタン・入力欄レイアウト。
8. **ダークモード**: 端末や好みに合わせてライト・ダークテーマを切り替える機能（設定はブラウザに自動保存）。

---

## 2. 使用技術
本システムはサーバーレスなSPA（シングルページアプリケーション）として構築されています。

- **コア言語**: HTML5, Vanilla CSS, Vanilla JavaScript (ES Modules)
- **バックエンド / データベース**: Firebase (v10.8.0)
  - **Firebase Authentication**: ユーザー認証管理
  - **Cloud Firestore**: スケジュールおよび日報データの永続化
- **ライブラリ**:
  - **SheetJS (xlsx.full.min.js)**: クライアントサイドでの高度なExcelファイル生成
- **インフラ・公開環境**:
  - **GitHub**: ソースコード管理 (`https://github.com/1105yuky00109-web/weekly-report-app`)
  - **Netlify**: ホスティング＆継続的デプロイ (CI/CD)

---

## 3. ビルド・デプロイルールと構成
セキュリティの観点から、APIキーなどのFirebase接続情報はソースコードに直接コミットせず、環境変数からビルド時に動的注入する仕組みを採用しています。

### ディレクトリ構成
```text
├── public/                 # 公開ディレクトリ (Netlify公開対象)
│   ├── index.html          # アプリケーションのメインHTML
│   ├── app.js              # フロントエンドロジック (ビルド時に接続キーが注入される)
│   └── style.css           # スタイルシート (ダークモード・iPad・印刷用定義含む)
├── build-inject.js         # ビルド時環境変数注入用Node.jsスクリプト
├── package.json            # プロジェクト設定 (ビルドスクリプト定義)
├── netlify.toml            # Netlify用ビルド・デプロイ設定
└── .gitignore              # Git管理対象外ファイル定義
```

### ビルドの仕組み
Netlifyでビルドが行われる際、`package.json` に定義された `npm run build` (内部的に `node build-inject.js` を実行) が走ります。
このスクリプトは、Netlify側に設定された環境変数から以下のFirebase設定を読み取り、`public/app.js` のプレースホルダー部分に注入します。

#### 設定が必要な環境変数 (Netlifyダッシュボード上)
- `FIREBASE_API_KEY`
- `FIREBASE_AUTH_DOMAIN`
- `FIREBASE_PROJECT_ID`
- `FIREBASE_STORAGE_BUCKET`
- `FIREBASE_MESSAGING_SENDER_ID`
- `FIREBASE_APP_ID`

---

## 4. 開発時のルール・規約
コードを編集・改修する際は以下のルールを厳守してください。

1. **言語設定**:
   - ソースコード内のコメントやDocstringは、原則として**日本語**で記述してください。
   - ユーザーとの対話、ドキュメントもすべて日本語を使用します。
2. **依存ライブラリの追加**:
   - 余計な依存パッケージ（Node.jsモジュール等）は極力追加せず、HTMLから直接CDNで読み込む軽量な構成を維持してください。
3. **レスポンシブデザインの維持**:
   - iPad（768px〜1024px）およびモバイル（600px以下）のレイアウトが崩れないよう、CSS編集時はメディアクエリの動作を確認してください。
   - タッチ操作時に押しづらくならないよう、クリックターゲット（ボタンや選択肢）は `40px` 以上の高さを確保してください。
4. **セキュリティルール**:
   - Firestoreは認証されたユーザーのみが読み書きできるように、Firebase側で適切なセキュリティルールを維持してください。
5. **Excel出力（ExcelJS）時のスタイル共有バグの防止**:
   - 既存のExcelテンプレート（`template-weekly.xlsx`など）を読み込んでセルのスタイル（塗りつぶしなど）を上書きする際、Excelの最適化仕様により、複数のセルが裏で「同じスタイルオブジェクト」を共有していることがあります。
   - スタイルを共有したまま1つのセルの `fill` や書式を変更すると、他の共有セルも連動して書き換わってしまいます。
   - これを防ぐため、ループ処理などでセルにスタイルを設定する際は、必ず事前に `cell.style = Object.create(cell.style);` を実行して、スタイル参照の共有を解除（デカップリング）してください。

---

## 5. アップデート履歴

### 2026年5月29日
*   **資格保有者サマリーパネルの完全復元**：
    *   工程管理表（ガントチャート）の直上に、以前大和田様と設計した薄いブルー背景・青枠線付きのサマリーパネル（`gantt-qual-panel`）を復元。
    *   `≪1級建築≫` `≪1級土木≫` `≪2級躯体≫` `≪実務経験≫` の短縮表記と、各メンバー名および「（支店専任）」「（非専任）」のカッコ書き表示を適用。
    *   Firestore の最新キー名（`q1b`, `q1c`, `q2b_躯体`, `exp`）に完全に一致するように集計ロジックを修正。
    *   支店フィルター（`gantt-branch-filter`）の選択値と動的に連動して人数・名前リストが切り替わる仕様を実装。
*   **印刷プレビューカラーの修復**：
    *   印刷用CSS（`@media print`）に設定を追加し、印刷プレビュー（PDF保存）の際にもガントチャートのバー（作業予定）や資格者パネルの背景色が真っ白にならず、鮮やかに印刷されるよう修復。
*   **バックアップの作成**：
    *   物理バックアップフォルダ（`weekly-report-app_backup_20260529`）を作成。
    *   Git コミットと Git タグ `stable-gantt-fix` を作成し、何があってもこの状態へ復元できるように保護。

### 2026年5月29日 (追加改修)
*   **上長承認フロー（予定・実績）の実装**：
    *   Firestore の `reports` コレクションの各ドキュメントに `planStatus` / `actualStatus` / `planRejectReason` / `actualRejectReason` を新設し、予定と実績を個別に承認・差し戻し（コメント付き）できる仕組みを構築。
    *   承認済みの状態では、一般社員側の入力エリアやボタンが編集ロック（無効化）されるよう制御を追加。
*   **FCM (Firebase Cloud Messaging) によるWebプッシュ通知の実装**：
    *   一般社員が週報（予定・実績）を提出した際、自動的に管理者のPCブラウザやスマホ（PWAインストール時）にプッシュ通知を送る Firestore トリガー関数 `onReportWrite` を `functions/index.js` に追加。
    *   フロントエンド側で FCM トークンの取得と Firestore (管理者 tokens、一般社員 tokens) への自動保存を実装。
    *   PWA化を推進するため、`manifest.json` の作成と HTML への読み込み設定、およびアプリアイコン画像の配置を完了。バッジAPIを統合し、アプリ起動時にアイコンの未読件数バッジが消える処理を追加。
*   **管理者用「提出状況確認・催促」UIの実装**：
    *   個人別一覧・レポート画面に管理者専用の「提出状況の確認と催促」パネルを新設。
    *   選択された週について、全員の「予定」および「実績」の提出・承認・差し戻し状況を一覧表示。
    *   未提出の社員に対して、ワンクリックでプッシュ通知（FCM）およびメールの双方で催促を送る Cloud Functions HTTPS API `sendRemindNotification` を実装し連携。

### 2026年5月29日 (承認UIのブラッシュアップ)
*   **印刷プレビュー画面（書類形式）での承認・差し戻しUIの統合**：
    *   管理者が「個人別一覧・レポート」でメンバーのA4縦の印刷プレビュー書類を確認しながら、その場で直接予定・実績の承認や差し戻しが行える「🛡️ 上長承認操作パネル」を印刷プレビュー枠の真下に実装。
    *   差し戻し時に「差し戻し理由（コメント）」を収集する入力フォームを内蔵。
    *   以前、入力画面（週の予定と実績タブ）に追加していた、切り替え時にバグの温床となっていた「表示対象の社員」プルダウンは廃止し、本来の「本人の週報を入力する画面」に差し戻し。

### 2026年5月30日
*   **予定・実績バッジのレイアウトズレ解消および印刷ボタンの右端寄せ**：
    *   予定・実績バッジの要素定義を `span` に統一し、不揃いな下マージン設定を整理することで上下中央の高さを完全に揃えるように修正。
    *   ステータスバッジと「🖨️ 印刷」ボタンを1つの Flexbox 行にまとめ、バッジを左側、印刷ボタンを右端に美しく配置。
    *   差し戻し警告エリア（`#report-reject-warning`）をあらかじめ静的 HTML 上に定義し、不要な DOM の動的挿入によるレイアウト崩れを防止。
*   **F5更新時の管理者画面一瞬チラつきバグの解消**：
    *   認証監視（`onAuthStateChanged`）にて、ログイン後の非同期処理（会社解決・メンバーロード・ロール判定など）がすべて完了した後にローディングを解除するよう遅延制御を実装。
    *   HTMLの初期アクティブタブを、管理者画面である「工事登録」から、全員共通の「工程管理表（`gantt-view`）」に変更し、安全性を二重に強化。
*   **月間個人別 工事稼働時間（集計）パネルの非表示化**：
    *   「個人別一覧・レポート」タブの下部に表示されていた「【月間】個人別 工事稼働時間（集計）」パネル（各担当者ごとの工事稼働時間表）を非表示（無効化）にするよう修正。
*   **管理者用パネルのタイトル変更**：
    *   「個人別一覧・レポート」画面にある管理者用パネル「提出状況の確認と催促」の見出しタイトルを「提出状況の確認」に変更。
*   **印刷用CSSボーダー調整の保存**：
    *   印刷用テーブルの境界線を整理し、一部の重複ボーダーを非表示にする調整を `public/style-v4.css` に反映。
*   **バックアップと安定版タグの作成**：
    *   最新の正常動作する状態を保護するため、Git タグ `stable-layout-fix` を作成し GitHub に保存。
